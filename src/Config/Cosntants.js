/**
 * size of matrix (NxN)
 */
const sizeMatrix = 6;
/**
 * Valid characters for the dna
 */
const validCharacters = ['A', 'T', 'C', 'G'];
/**
 * more than N letter sequence equal.
 */
const sequenceMutant = 4;

module.exports = {
    sizeMatrix,
    validCharacters,
    sequenceMutant
}