/**
 * size of matrix (NxN).
 */
const sizeMatrix = 6;
/**
 * Valid characters for the dna.
 */
const validCharactersEnum = ['A', 'T', 'C', 'G'];

/**
 * letter rows.
 */
const letterRows = 4;
/**
 * more than N sequence.
 */
const sequenceMutant = 2;

module.exports = {
    sizeMatrix,
    validCharactersEnum,
    sequenceMutant,
    letterRows
}